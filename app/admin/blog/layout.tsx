import { ReactNode } from "react"

export const metadata = {
  title: "Blog CMS | Swopify Admin",
  description: "Manage Swopify blog posts and content.",
}

export default function BlogLayout({ children }: { children: ReactNode }) {
  return children
}
